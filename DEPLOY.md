# 🚀 Инструкция по деплою Nano Banana

## Быстрый старт на Render.com (5 минут)

### Шаг 1: Подготовка API ключей

#### Telegram Bot Token
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте: `/newbot`
3. Придумайте имя: `My Nano Banana Bot`
4. Придумайте username: `my_nano_banana_bot` (должен заканчиваться на `_bot`)
5. Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
6. **Важно**: Настройте платежи Stars:
   - Отправьте `/mybots`
   - Выберите вашего бота
   - Нажмите "Payments"
   - Выберите "Telegram Stars"
   - Подтвердите

#### Gemini API Key
1. Откройте [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите через Google аккаунт
3. Нажмите "Create API Key"
4. Скопируйте ключ (выглядит как `AIzaSy...`)

---

### Шаг 2: Загрузка на GitHub

1. Создайте новый репозиторий на GitHub
2. Загрузите все файлы проекта (кроме `.env`!)

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/nano-banana.git
git push -u origin main
```

---

### Шаг 3: Деплой на Render.com

1. **Зарегистрируйтесь** на [Render.com](https://render.com) (можно через GitHub)

2. **Создайте Web Service**:
   - Нажмите "New +" → "Web Service"
   - Выберите "Connect a repository"
   - Подключите GitHub и выберите ваш репозиторий

3. **Настройте сервис**:
   ```
   Name: nano-banana (или любое имя)
   Region: Frankfurt (или ближайший к вам)
   Branch: main
   Root Directory: (оставьте пустым)
   Environment: Node
   Build Command: npm install
   Start Command: npm start
   Plan: Free (или Starter за $7/месяц)
   ```

4. **Добавьте Environment Variables**:
   
   Нажмите "Advanced" → "Add Environment Variable"
   
   Добавьте эти переменные:
   
   | Key | Value |
   |-----|-------|
   | `TELEGRAM_BOT_TOKEN` | Ваш токен от BotFather |
   | `GEMINI_API_KEY` | Ваш ключ от Google AI |
   | `NODE_ENV` | `production` |
   | `PORT` | `3000` |
   | `FREE_TOKENS` | `100` |
   | `TOKENS_PER_STAR` | `1000` |

5. **Создайте сервис**: Нажмите "Create Web Service"

6. **Дождитесь деплоя**: Обычно занимает 3-5 минут

7. **Получите URL**: После деплоя вы получите URL вида `https://nano-banana-abc123.onrender.com`

---

### Шаг 4: Финальные настройки

#### Обновите ссылку на бота в веб-интерфейсе

1. В GitHub откройте файл `public/index.html`
2. Найдите строку:
   ```html
   <a href="https://t.me/YOUR_BOT_USERNAME"
   ```
3. Замените `YOUR_BOT_USERNAME` на username вашего бота
4. Закоммитьте изменения
5. Render автоматически задеплоит обновление

#### Протестируйте

1. **Веб-интерфейс**: Откройте ваш URL в браузере
2. **Telegram бот**: Найдите бота и отправьте `/start`

---

## Альтернатива: Railway.app

### Преимущества
- ✅ Проще в использовании
- ✅ Быстрый деплой
- ✅ $5 бесплатно каждый месяц

### Инструкция

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Node.js
6. Добавьте переменные окружения в Settings → Variables:
   - `TELEGRAM_BOT_TOKEN`
   - `GEMINI_API_KEY`
   - `NODE_ENV=production`
   - `PORT=3000`
7. Готово! Railway даст вам URL

---

## Альтернатива: VDS/VPS (Для опытных)

### Провайдеры
- **VDS.RU** - от 200₽/месяц (Россия)
- **Timeweb** - от 200₽/месяц (Россия)
- **DigitalOcean** - от $4/месяц
- **Hetzner** - от €3.79/месяц

### Быстрая установка на Ubuntu

```bash
# Подключитесь по SSH
ssh root@your-server-ip

# Обновите систему
apt update && apt upgrade -y

# Установите Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# Установите Git
apt install -y git

# Клонируйте проект
cd /var/www
git clone https://github.com/your-username/nano-banana.git
cd nano-banana

# Установите зависимости
npm install

# Создайте .env файл
nano .env
```

Вставьте в `.env`:
```env
TELEGRAM_BOT_TOKEN=ваш_токен
GEMINI_API_KEY=ваш_ключ
PORT=3000
NODE_ENV=production
TOKENS_PER_STAR=1000
FREE_TOKENS=100
```

Сохраните (Ctrl+X, Y, Enter)

```bash
# Установите PM2 для автозапуска
npm install -g pm2

# Запустите приложение
pm2 start server.js --name nano-banana

# Настройте автозапуск
pm2 startup
pm2 save

# Проверьте статус
pm2 status
```

### Настройка Nginx (для доменного имени)

```bash
# Установите Nginx
apt install -y nginx

# Создайте конфиг
nano /etc/nginx/sites-available/nano-banana
```

Вставьте:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
# Активируйте конфиг
ln -s /etc/nginx/sites-available/nano-banana /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx

# Установите SSL (опционально, но рекомендуется)
apt install -y certbot python3-certbot-nginx
certbot --nginx -d your-domain.com
```

---

## Проверка работоспособности

### Веб-интерфейс
Откройте в браузере: `https://your-app.onrender.com`

Вы должны увидеть:
- ✅ Красивый интерфейс с бананом
- ✅ Баланс токенов: 100
- ✅ Форму для ввода запроса

### Telegram бот
1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Вы должны получить приветственное сообщение
4. Отправьте любой текст для генерации

### API Health Check
```bash
curl https://your-app.onrender.com/health
```

Должно вернуть: `{"status":"ok","timestamp":"..."}`

---

## Мониторинг логов

### Render.com
1. Откройте Dashboard
2. Выберите ваш сервис
3. Перейдите в "Logs"

### Railway.app
1. Откройте проект
2. Нажмите на сервис
3. Вкладка "Deployments" → нажмите на активный деплой

### VPS с PM2
```bash
# Просмотр логов
pm2 logs nano-banana

# Только ошибки
pm2 logs nano-banana --err

# Последние 100 строк
pm2 logs nano-banana --lines 100
```

---

## Обновление проекта

### На Render.com / Railway
Просто сделайте push в GitHub:
```bash
git add .
git commit -m "Update"
git push
```

Render/Railway автоматически задеплоят изменения.

### На VPS
```bash
cd /var/www/nano-banana
git pull
npm install
pm2 restart nano-banana
```

---

## Решение проблем

### Ошибка "Application failed to respond"

**Причина**: Порт не настроен правильно

**Решение**: Убедитесь, что переменная `PORT` установлена в `3000`

### Telegram бот не отвечает

**Проверьте**:
1. ✅ `TELEGRAM_BOT_TOKEN` правильный
2. ✅ Сервер запущен и отвечает на `/health`
3. ✅ В логах нет ошибок

**Лог должен показывать**:
```
✅ База данных инициализирована
🤖 Telegram бот запущен
✅ Telegram бот инициализирован
```

### Ошибка Gemini API

**Причина**: Неправильный ключ или исчерпана квота

**Решение**: 
1. Проверьте `GEMINI_API_KEY`
2. Зайдите на [Google AI Studio](https://makersuite.google.com/)
3. Проверьте, не превышен ли лимит запросов

### База данных сбрасывается на Render.com (Free Plan)

**Причина**: Free план не сохраняет файлы

**Решения**:
1. Перейдите на Starter план ($7/месяц) с Persistent Disk
2. Используйте внешнюю БД (PostgreSQL)
3. Для тестирования - это нормально

---

## Настройка для продакшена

### 1. Используйте PostgreSQL (вместо SQLite)

```bash
npm install pg
```

Обновите `database.js` для работы с PostgreSQL

### 2. Добавьте rate limiting

```bash
npm install express-rate-limit
```

### 3. Настройте мониторинг

Используйте сервисы:
- **Sentry** для отслеживания ошибок
- **Uptimerobot** для проверки аптайма
- **LogTail** для централизованных логов

---

## Стоимость работы

### Бесплатный вариант
- **Render.com Free**: ✅ Спит после 15 минут неактивности
- **Railway**: ✅ $5 кредитов в месяц
- **Fly.io**: ✅ 3 маленькие машины бесплатно

### Платные варианты
- **Render.com Starter**: $7/месяц + $1/GB Persistent Disk
- **Railway Pay-as-you-go**: ~$5-10/месяц
- **DigitalOcean Droplet**: $4/месяц
- **VDS.RU**: от 200₽/месяц

---

## Готово! 🎉

Теперь у вас работающее приложение!

**Что дальше?**
1. ✅ Настройте красивое доменное имя
2. ✅ Продвигайте вашего бота
3. ✅ Мониторьте продажи токенов
4. ✅ Масштабируйте при необходимости

**Вопросы?** Создайте Issue на GitHub или напишите в поддержку!

