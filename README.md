# 🍌 Nano Banana - Gemini AI Platform

Полнофункциональная платформа для генерации текста с помощью Google Gemini AI. Включает веб-интерфейс и Telegram бота с оплатой через Telegram Stars.

## 🚀 Возможности

- 🌐 **Веб-интерфейс** - красивый и удобный UI для генерации текста
- 🤖 **Telegram Бот** - генерация прямо в мессенджере
- ⭐ **Оплата Stars** - покупка токенов через Telegram Stars
- 💎 **Система токенов** - гибкая система начисления и списания
- 📊 **История** - сохранение всех генераций и транзакций
- 🗄️ **SQLite База данных** - легкая и быстрая БД

## 📋 Требования

- Node.js 16+ 
- NPM или Yarn
- Telegram Bot Token (от @BotFather)
- Gemini API Key (от Google AI Studio)

## 🔧 Установка

### 1. Клонируйте репозиторий или скачайте файлы

```bash
cd nano-banana
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Создайте файл `.env`

Скопируйте `env.example` в `.env` и заполните:

```bash
cp env.example .env
```

Отредактируйте `.env`:

```env
# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Gemini API Key (получите на https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=AIzaSy...

# Настройки сервера
PORT=3000
NODE_ENV=production

# Настройки токенов
TOKENS_PER_STAR=1000
FREE_TOKENS=100
```

### 4. Запустите сервер

```bash
# Для разработки
npm run dev

# Для продакшена
npm start
```

Сервер запустится на `http://localhost:3000`

## 🎯 Как получить API ключи

### Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям (выберите имя и username)
4. Скопируйте полученный токен в `.env`
5. Настройте платежи: `/mybots` → выберите бота → `Payments` → подключите Telegram Stars

### Gemini API Key

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите с Google аккаунтом
3. Нажмите "Get API Key" или "Create API Key"
4. Скопируйте ключ в `.env`

## 🌐 Деплой на Render.com (Рекомендуется)

### Подготовка

1. Создайте репозиторий на GitHub
2. Загрузите туда код проекта (без `.env` файла!)

### Деплой

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройте:
   - **Name**: nano-banana (или любое имя)
   - **Environment**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free (или платный)

5. Добавьте Environment Variables (переменные окружения):
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен
   GEMINI_API_KEY=ваш_ключ
   PORT=3000
   NODE_ENV=production
   TOKENS_PER_STAR=1000
   FREE_TOKENS=100
   ```

6. Нажмите "Create Web Service"
7. Дождитесь деплоя (3-5 минут)
8. Ваш сайт будет доступен по адресу: `https://your-app.onrender.com`

### Настройка Telegram бота после деплоя

После деплоя обновите ссылку на бота в файле `public/index.html`:

```html
<a href="https://t.me/YOUR_BOT_USERNAME" ...>
```

## 🚀 Деплой на другие платформы

### Railway.app

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Добавьте переменные окружения
5. Railway автоматически определит Node.js и задеплоит

### VDS/VPS (Linux)

```bash
# Подключитесь к серверу по SSH
ssh user@your-server.com

# Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Клонируйте проект
git clone your-repo-url
cd nano-banana

# Установите зависимости
npm install

# Создайте .env файл
nano .env
# (вставьте ваши ключи)

# Установите PM2 для автозапуска
sudo npm install -g pm2

# Запустите приложение
pm2 start server.js --name nano-banana

# Настройте автозапуск при перезагрузке
pm2 startup
pm2 save
```

### Настройка Nginx (опционально, для VPS)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 📱 Использование Telegram Бота

### Команды:

- `/start` - Начать работу (получить бесплатные токены)
- `/balance` - Проверить баланс токенов
- `/buy` - Купить токены за Stars
- `/history` - Посмотреть историю генераций
- `/help` - Справка

### Генерация текста:

Просто отправьте боту любой текст, и он сгенерирует ответ!

### Покупка токенов:

1. Отправьте `/buy`
2. Выберите пакет токенов
3. Оплатите через Telegram Stars
4. Токены мгновенно зачислятся на баланс

## 🌐 Использование Веб-интерфейса

1. Откройте сайт в браузере
2. Вам автоматически выдадут 100 бесплатных токенов
3. Введите запрос в текстовое поле
4. Нажмите "Генерировать"
5. Получите ответ от AI

## 🛠️ Технологии

- **Backend**: Node.js, Express
- **Database**: SQLite (better-sqlite3)
- **AI**: Google Gemini API
- **Telegram**: node-telegram-bot-api
- **Frontend**: Vanilla JavaScript, HTML5, CSS3

## 📊 Структура проекта

```
nano-banana/
├── server.js           # Основной сервер
├── database.js         # Работа с БД
├── gemini-service.js   # Gemini API
├── telegram-bot.js     # Telegram бот
├── package.json        # Зависимости
├── env.example         # Пример переменных окружения
├── public/            
│   ├── index.html      # Главная страница
│   ├── style.css       # Стили
│   └── app.js          # Frontend логика
└── README.md           # Документация
```

## 💡 Советы

### Оптимизация затрат на токены

- Настройте `TOKENS_PER_STAR` в зависимости от ваших цен
- Предлагайте бонусы при покупке больших пакетов (уже реализовано)
- Давайте бесплатные токены новым пользователям

### Мониторинг

Используйте endpoint `/health` для проверки работоспособности:

```bash
curl https://your-app.onrender.com/health
```

### Логи

Для просмотра логов на Render.com:
1. Откройте Dashboard
2. Выберите ваш сервис
3. Перейдите в "Logs"

## 🔒 Безопасность

- ✅ Никогда не коммитьте `.env` файл в Git
- ✅ Используйте переменные окружения на сервере
- ✅ Храните API ключи в секрете
- ✅ Регулярно обновляйте зависимости

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что сервер запущен
3. Проверьте логи: `pm2 logs nano-banana` (для VPS)

### Ошибка Gemini API

1. Проверьте `GEMINI_API_KEY`
2. Убедитесь, что API ключ активен
3. Проверьте квоту на [Google AI Studio](https://makersuite.google.com/)

### База данных не создается

1. Убедитесь, что у приложения есть права на запись
2. На Render.com база будет сбрасываться при каждом деплое (используйте Render Disk для постоянства)

## 📈 Масштабирование

Для увеличения нагрузки:

1. **Используйте PostgreSQL** вместо SQLite для production
2. **Redis** для кеширования частых запросов
3. **Load Balancer** для распределения нагрузки
4. **CDN** для статических файлов

## 📄 Лицензия

MIT License - используйте свободно!

## 🤝 Поддержка

Если у вас возникли вопросы:
- Создайте Issue на GitHub
- Напишите в Telegram: @your_username

## 🎉 Готово!

Теперь у вас есть полноценная платформа для монетизации Gemini API через Telegram!

**Следующие шаги:**
1. ✅ Получите API ключи
2. ✅ Настройте `.env`
3. ✅ Задеплойте на Render.com
4. ✅ Обновите ссылку на бота в HTML
5. ✅ Начните продавать токены! 🚀

