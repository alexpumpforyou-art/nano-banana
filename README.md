# 🍌 Nano Banana - AI Platform

Мощная платформа для генерации текста и изображений с использованием Google Gemini и Imagen. Включает в себя Telegram-бота, веб-админ панель, систему очередей для масштабируемости и интеграцию платежей.

## 🚀 Возможности

### 🤖 Telegram Бот
- **Генерация текста**: Умные ответы с поддержкой контекста (Gemini Pro/Flash).
- **Генерация изображений**: Создание картинок по описанию (Imagen 3/4, Gemini).
- **Редактирование изображений**: Изменение картинок текстовыми командами.
- **Анимация статусов**: "Думаю...", "Рисую..." для лучшего UX.
- **Реферальная система**: Бонусы за приглашение друзей.

### 💳 Платежная система
- **Telegram Stars**: Оплата цифровых товаров внутри Telegram.
- **ЮKassa**: Прием платежей в рублях (банковские карты, SberPay и др.).
- **Система кредитов**: Гибкая валюта (1 кредит = 1 текст, 2 кредита = 1 картинка).

### ⚙️ Архитектура и Админка
- **Масштабируемость**: Тяжелые задачи (генерация картинок) вынесены в фоновые воркеры через Redis.
- **PostgreSQL**: Надежная база данных для пользователей и транзакций.
- **Веб-админка**:
  - Статистика (пользователи, генерации, доход).
  - Управление пользователями (бан, начисление кредитов, сообщения).
  - Просмотр истории запросов (Lazy Load для картинок).
  - Управление контентом бота.
  - Массовые рассылки.

---

## 🛠️ Технологический стек

- **Backend**: Node.js, Express
- **Database**: PostgreSQL (Knex.js ORM)
- **Queue**: Redis + BullMQ (для фоновых задач)
- **AI**: Google Gemini API, Google Imagen
- **Telegram**: node-telegram-bot-api
- **Frontend (Admin)**: Vanilla JS, HTML5, CSS3

---

## 📋 Требования

- **Node.js**: v18+
- **PostgreSQL**: v14+
- **Redis**: v6+
- **API Keys**:
  - Telegram Bot Token
  - Google Gemini API Key
  - YooKassa Shop ID & Secret (опционально)

---

## 🔧 Установка и запуск (Локально)

### 1. Клонирование и зависимости
```bash
git clone <repository-url>
cd nano-banana
npm install
```

### 2. Настройка окружения
Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```
Заполните необходимые переменные (см. раздел "Переменные окружения").

### 3. Запуск базы данных и Redis
Убедитесь, что у вас запущены PostgreSQL и Redis.
Настройте подключение в `.env` (`DATABASE_URL`, `REDIS_URL`).

### 4. Миграции
При первом запуске скрипт `start` автоматически применит миграции.
Вручную:
```bash
npx knex migrate:latest
```

### 5. Запуск
```bash
# Запуск сервера (API + Бот) и Воркера
npm start

# Или раздельно:
# Терминал 1 (Сервер):
node server.js
# Терминал 2 (Воркер):
node worker.js
```

---

## ☁️ Деплой на Railway (Рекомендуется)

Проект полностью настроен для деплоя на Railway.app.

1. **Создайте проект** в Railway.
2. **Добавьте базу данных**: PostgreSQL.
3. **Добавьте Redis**.
4. **Подключите GitHub репозиторий**.
5. **Настройте переменные окружения** (Variables) в сервисе приложения:
   - Скопируйте значения из вашего `.env`.
   - `DATABASE_URL` и `REDIS_URL` обычно подтягиваются автоматически, если сервисы в одном проекте.
   - **ВАЖНО**: Установите `NIXPACKS_START_CMD` = `npm start` (это запустит и миграции, и сервер).
6. **Воркер**:
   - В Railway можно запустить воркер как отдельный сервис или в том же контейнере (через `concurrently` или `pm2`, если настроить).
   - **Рекомендуемый способ**: Добавить еще один сервис из того же репо, но с командой запуска `node worker.js`. Это позволит масштабировать воркеры независимо от основного бота.

---

## 🔑 Переменные окружения (.env)

| Переменная | Описание | Пример |
|------------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | `123456:ABC-DEF...` |
| `GEMINI_API_KEY` | Ключ от Google AI Studio | `AIzaSy...` |
| `DATABASE_URL` | Строка подключения к PostgreSQL | `postgresql://user:pass@host:5432/db` |
| `REDIS_URL` | Строка подключения к Redis | `redis://:pass@host:6379` |
| `ADMIN_PASSWORD` | Пароль для входа в админку | `supersecretpass` |
| `YOOKASSA_SHOP_ID` | ID магазина ЮKassa | `123456` |
| `YOOKASSA_SECRET_KEY` | Секретный ключ ЮKassa | `test_...` |
| `YOOKASSA_PROVIDER_TOKEN`| Токен провайдера в BotFather | `381764678:TEST:...` |
| `ADMIN_TELEGRAM_ID` | Ваш ID в Telegram (для уведомлений) | `123456789` |

---

## 📂 Структура проекта

```
nano-banana/
├── server.js             # Основной сервер (Express API + Admin routes)
├── worker.js             # Воркер для обработки очереди генерации изображений
├── telegram-bot.js       # Логика Telegram бота
├── database-postgres.js  # Запросы к PostgreSQL (Knex)
├── queue-service.js      # Настройка очереди BullMQ
├── image-service.js      # Логика работы с Gemini/Imagen (генерация, редактирование)
├── gemini-service.js     # Логика работы с Gemini (текст)
├── yookassa-service.js   # Интеграция платежей
├── knexfile.js           # Конфигурация миграций БД
├── migrations/           # Файлы миграций SQL
├── public/               # Статика для админ-панели (HTML, CSS, JS)
│   └── admin.html        # SPA админ-панели
└── auto-migrate.js       # Скрипт авто-миграции при старте
```

## 🐛 Решение проблем

**1. Бот не отвечает**
- Проверьте логи: `npm start` или логи в Railway.
- Убедитесь, что `TELEGRAM_BOT_TOKEN` верный.
- Если используете Polling (локально), убедитесь, что не запущен другой экземпляр.

**2. Картинки не генерируются**
- Проверьте, запущен ли `worker.js`.
- Проверьте подключение к Redis.
- Проверьте `GEMINI_API_KEY` и доступность моделей Imagen/Gemini.

**3. Ошибка подключения к БД**
- Проверьте `DATABASE_URL`.
- Для Railway убедитесь, что в `knexfile.js` включен SSL (`rejectUnauthorized: false`), это уже настроено в репозитории.

**4. Админка не грузит запросы**
- Мы оптимизировали загрузку: картинки теперь грузятся по кнопке "Просмотр". Если что-то не работает, очистите кэш браузера.
