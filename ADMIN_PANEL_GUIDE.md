# 🍌 Руководство по Админ-Панели

## 📋 Описание

Веб-админ панель для управления пользователями и мониторинга активности бота.

## 🔐 Доступ

1. **Установите пароль** в переменных окружения Railway:
   ```
   ADMIN_PASSWORD=ваш_надежный_пароль
   ```

2. **Откройте админ-панель**:
   ```
   https://ваш-домен.railway.app/admin
   ```

3. **Войдите** используя установленный пароль.

## ✨ Возможности

### 📊 Дашборд со статистикой
- Общее количество пользователей
- Количество Telegram и Web пользователей
- Общее количество генераций
- Потрачено кредитов
- Заблокированные пользователи
- Получено Telegram Stars

### 👥 Управление пользователями
- **Поиск** по username, ID, Telegram ID, referral code
- **Фильтры**:
  - Все пользователи
  - Только Telegram
  - Только Web
  - Заблокированные

### 📈 Детальная информация о пользователе
При клике на "Подробнее" отображается:
- Основная информация (ID, username, баланс, статус)
- Статистика (генерации, потрачено, рефералы)
- История генераций (последние 10)
- История транзакций (последние 10)
- Список рефералов

## 🔒 Безопасность

- **Сессии**: Автоматический выход через 24 часа неактивности
- **Пароль**: Хранится в переменных окружения (не в коде)
- **Защита роутов**: Все API endpoints требуют валидную сессию

## 🛠️ Технические детали

### API Endpoints

- `POST /api/admin/login` - Вход в админ-панель
- `GET /api/admin/stats` - Общая статистика
- `GET /api/admin/users` - Список всех пользователей
- `GET /api/admin/user/:id` - Детальная информация о пользователе

### Переменные окружения

```env
ADMIN_PASSWORD=your_secure_password_here
```

## 📝 Примечания

- Сессия сохраняется в `localStorage` браузера
- При истечении сессии требуется повторный вход
- Все данные обновляются в реальном времени при загрузке страницы

## 🚀 Быстрый старт

1. Добавьте `ADMIN_PASSWORD` в Railway
2. Перезапустите приложение
3. Откройте `/admin` в браузере
4. Войдите с паролем
5. Наслаждайтесь управлением! 🎉

