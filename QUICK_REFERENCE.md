# ⚡ Быстрая шпаргалка - Nano Banana Bot

Краткий справочник по самым частым изменениям.

---

## 🎯 Самые частые изменения

### 💰 Изменить цены

**Файл:** `telegram-bot.js` (строки 17-23)

```javascript
const PRICES = {
  TEXT_SHORT: 1,      // ← Измените здесь
  TEXT_LONG: 2,       // ← И здесь
  IMAGE_GEN: 10,      // ← И здесь
  IMAGE_EDIT: 15      // ← И здесь
};
```

---

### 🎁 Изменить бесплатные кредиты

**Файл:** `telegram-bot.js` (строка 12)

```javascript
const FREE_CREDITS = 10;  // ← Измените на нужное число
```

---

### 🔗 Изменить реферальный бонус

**Файл:** `telegram-bot.js` (строка 14)

```javascript
const REFERRAL_BONUS = 5;  // ← Измените на нужное число
```

---

### 📝 Изменить приветствие

**Файл:** `telegram-bot.js` (строка ~134)

Найдите:
```javascript
const welcomeText = `
🍌 ${isNewUser ? 'Добро пожаловать' : 'С возвращением'} в Nano Banana!
```

Замените на свой текст.

---

### 📱 Добавить кнопку в главное меню

**Файл:** `telegram-bot.js` (строка ~144)

```javascript
const keyboard = {
  inline_keyboard: [
    [
      { text: '🎨 Ваш текст', callback_data: 'menu_ваш_id' },
      { text: '💎 Баланс', callback_data: 'menu_balance' }
    ],
    // Добавьте новую строку:
    [
      { text: '🆕 Новая кнопка', callback_data: 'menu_new' }
    ],
  ]
};
```

Затем добавьте обработчик в секции `bot.on('callback_query')`:

```javascript
} else if (data === 'menu_new') {
  await bot.answerCallbackQuery(query.id);
  await bot.sendMessage(chatId, 'Вы нажали новую кнопку!');
}
```

---

## 🔧 Команды Git

### Сохранить изменения
```bash
git add .
git commit -m "описание изменений"
git push
```

### Откатить последний коммит
```bash
git reset --soft HEAD~1
```

### Посмотреть изменения
```bash
git diff
```

---

## 🚀 Railway

### Смотреть логи
1. Railway.app → ваш проект
2. Deployments → View Logs

### Перезапустить
1. Railway.app → ваш проект
2. Settings → Restart

### Изменить переменные
1. Railway.app → ваш проект
2. Variables → редактируйте

---

## 📊 Важные переменные окружения (Railway)

```
TELEGRAM_BOT_TOKEN=ваш_токен
GEMINI_API_KEY=ваш_ключ
FREE_CREDITS=10
CREDITS_PER_STAR=40
REFERRAL_BONUS=5
ADMIN_TELEGRAM_ID=ваш_telegram_id
NODE_ENV=production
```

---

## 🐛 Если бот не работает

1. **Проверьте логи Railway**
2. **Убедитесь что одна копия бота работает** (не запускайте локально)
3. **Проверьте переменные окружения**
4. **Restart на Railway**

---

## 📞 Полезные команды бота (для администратора)

```
/adminadd TELEGRAM_ID CREDITS  - Начислить кредиты
/adminuser TELEGRAM_ID          - Инфо о пользователе
/adminblock TELEGRAM_ID         - Заблокировать
/adminunblock TELEGRAM_ID       - Разблокировать
/stats                          - Статистика
```

---

## 🎨 Структура файлов (что где находится)

| Файл | За что отвечает |
|------|----------------|
| `telegram-bot.js` | Логика бота, команды, кнопки |
| `image-service.js` | Генерация изображений |
| `gemini-service.js` | Генерация текста |
| `database.js` | База данных |
| `server.js` | Веб-сервер |
| `.env` | Секретные ключи (НЕ заливать в git!) |

---

## 💡 Быстрые решения

### Добавить промокод
```javascript
// В telegram-bot.js добавьте:
bot.onText(/\/promo\s+(\S+)/, async (msg, match) => {
  const code = match[1];
  if (code === 'WELCOME') {
    // Начислите бонус
  }
});
```

### Ежедневный бонус
```javascript
bot.onText(/\/daily/, async (msg) => {
  // Проверьте последнее получение
  // Начислите 3-5 кредитов
});
```

### Показать примеры
```javascript
await bot.sendPhoto(chatId, 'URL_OR_FILE_ID', {
  caption: 'Пример работы'
});
```

---

**Подробнее смотрите в `BOT_CUSTOMIZATION_GUIDE.md`** 📚


