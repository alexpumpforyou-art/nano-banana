# 🔧 Решение проблемы с Gemini API

## ❌ Проблема

```
User location is not supported for the API use.
```

Gemini API заблокирован в вашем регионе (Россия/СНГ).

## ✅ Решения

### Вариант 1: VPN (Рекомендуется для тестирования)

1. **Установите VPN** (любой из):
   - Windscribe (бесплатно 10GB)
   - ProtonVPN (бесплатно)
   - TunnelBear (бесплатно 500MB)
   
2. **Подключитесь** к серверу в:
   - США
   - Европа (Германия, Франция)
   - Сингапур

3. **Перезапустите сервер**:
   ```bash
   Ctrl+C  # Остановить
   npm start  # Запустить снова
   ```

4. **Протестируйте**:
   ```bash
   node test-gemini.js
   ```

### Вариант 2: Прокси в коде (Для продакшена)

Обновим `gemini-service.js` для работы через прокси:

```javascript
const { GoogleGenerativeAI } = require('@google/generative-ai');
const { HttpsProxyAgent } = require('https-proxy-agent');

class GeminiService {
  constructor(apiKey, proxyUrl) {
    const options = {};
    
    if (proxyUrl) {
      // Настройка прокси
      options.fetchOptions = {
        agent: new HttpsProxyAgent(proxyUrl)
      };
    }
    
    this.genAI = new GoogleGenerativeAI(apiKey);
    this.model = this.genAI.getGenerativeModel({ 
      model: 'gemini-1.5-flash'  // Новая модель
    });
  }
  
  // ... остальной код
}
```

Установка:
```bash
npm install https-proxy-agent
```

В `.env` добавить:
```env
PROXY_URL=http://your-proxy-server:port
```

### Вариант 3: Деплой на Railway (Работает без VPN!)

**Лучшее решение!** Railway находится за границей, там Gemini работает:

1. Запустите:
   ```bash
   railway-deploy.bat
   ```

2. Или вручную:
   - Загрузите на GitHub
   - Задеплойте на Railway
   - Gemini будет работать автоматически!

### Вариант 4: Использовать альтернативу Gemini

Можем переключиться на:
- **OpenAI GPT** (работает из России через API)
- **Anthropic Claude** (через VPN)
- **YandexGPT** (работает без ограничений)
- **GigaChat** (Сбер, работает в России)

## 🎯 Рекомендация

**Для тестирования:**
- Используйте VPN → самое простое

**Для продакшена:**
- Задеплойте на Railway → работает из коробки
- Или используйте российский API (YandexGPT, GigaChat)

## 📋 Переключение на YandexGPT (работает в России)

Хотите, чтобы я переделал проект на YandexGPT?
Это займёт 5 минут и будет работать без VPN!

```javascript
// Вместо Gemini используем YandexGPT
// Получите ключ: https://cloud.yandex.ru/docs/iam/operations/api-key/create
```

## ❓ Что выбрать?

1. **Быстро протестировать** → VPN + перезапуск сервера
2. **Запустить в продакшен** → Деплой на Railway
3. **Работать в России долгосрочно** → YandexGPT или GigaChat

Что выберете?

