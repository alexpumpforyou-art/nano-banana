# 🚀 Деплой Nano Banana на Railway (пошагово)

## ✅ У вас уже есть:
- Аккаунт на Railway
- Рабочий проект локально
- API ключи (Telegram + Gemini)

## 📝 Способ 1: Через GitHub (Рекомендуется)

### Шаг 1: Создайте GitHub репозиторий

1. Откройте: https://github.com/new
2. Название: `nano-banana`
3. Приватность: **Public** или **Private** (не важно)
4. **НЕ создавайте** README, .gitignore, license
5. Нажмите "Create repository"

### Шаг 2: Загрузите код на GitHub

Откройте терминал в папке проекта и выполните:

```bash
git init
git add .
git commit -m "Initial commit: Nano Banana"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/nano-banana.git
git push -u origin main
```

⚠️ Замените `ВАШ_USERNAME` на ваш GitHub username!

### Шаг 3: Деплой на Railway

1. Откройте: https://railway.app/new
2. Нажмите: **"Deploy from GitHub repo"**
3. Если первый раз - разрешите доступ к GitHub
4. Выберите репозиторий: **nano-banana**
5. Railway автоматически определит Node.js и начнёт деплой

### Шаг 4: Добавьте Environment Variables

1. В Railway Dashboard нажмите на ваш сервис
2. Перейдите на вкладку **"Variables"**
3. Нажмите **"New Variable"** и добавьте по одной:

```
TELEGRAM_BOT_TOKEN
8526959887:AAF7bdIE1R-VUwmb7UEG8V3mUK43HDCdXyM

GEMINI_API_KEY
AIzaSyC0HQoaMkFW9slsVtCAsNtZdio3uSUUPcI

NODE_ENV
production

PORT
3000

FREE_TOKENS
100

TOKENS_PER_STAR
1000
```

4. Railway автоматически передеплоит с новыми переменными

### Шаг 5: Получите публичный URL

1. В Railway нажмите на ваш сервис
2. Перейдите в **"Settings"**
3. Найдите раздел **"Networking"** или **"Domains"**
4. Нажмите **"Generate Domain"**
5. Скопируйте URL (например: `nano-banana-production.up.railway.app`)

### Шаг 6: Обновите ссылку на бота

1. В вашем репозитории откройте файл: `public/index.html`
2. Найдите строку (около line 72):
```html
<a href="https://t.me/YOUR_BOT_USERNAME"
```
3. Замените `YOUR_BOT_USERNAME` на username вашего бота (без @)
4. Закоммитьте изменения:
```bash
git add public/index.html
git commit -m "Update bot link"
git push
```
5. Railway автоматически задеплоит обновление

## ✅ Готово!

Откройте ваш Railway URL в браузере - сайт работает!
Найдите бота в Telegram - бот работает!

---

## 📝 Способ 2: Без GitHub (Railway CLI)

Если не хотите использовать GitHub:

### Установите Railway CLI

```bash
npm install -g @railway/cli
```

### Авторизуйтесь

```bash
railway login
```

Откроется браузер для авторизации

### Инициализируйте проект

```bash
railway init
```

Выберите "Create new project"

### Загрузите код

```bash
railway up
```

### Добавьте переменные

```bash
railway variables set TELEGRAM_BOT_TOKEN=8526959887:AAF7bdIE1R-VUwmb7UEG8V3mUK43HDCdXyM
railway variables set GEMINI_API_KEY=AIzaSyC0HQoaMkFW9slsVtCAsNtZdio3uSUUPcI
railway variables set NODE_ENV=production
railway variables set PORT=3000
railway variables set FREE_TOKENS=100
railway variables set TOKENS_PER_STAR=1000
```

### Получите URL

```bash
railway domain
```

---

## 🔧 Полезные команды Railway CLI

```bash
# Посмотреть логи
railway logs

# Открыть проект в браузере
railway open

# Подключиться к shell
railway shell

# Посмотреть статус
railway status

# Удалить проект
railway down
```

---

## 🐛 Решение проблем

### Деплой завершается с ошибкой

**Проверьте логи:**
1. В Railway Dashboard → ваш сервис → "Deployments"
2. Нажмите на последний деплой
3. Посмотрите логи

**Частые проблемы:**
- Отсутствуют переменные окружения
- Неправильный Start Command (должен быть `npm start`)
- Build Command должен быть `npm install`

### Бот не отвечает

1. Проверьте, что сервис запущен (зелёный статус в Railway)
2. Откройте URL в браузере - должен показывать сайт
3. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
4. В логах должно быть: `🤖 Telegram бот запущен`

### База данных сбрасывается

Railway удаляет файлы при каждом деплое.

**Решение:**
1. В Railway перейдите в Settings
2. Добавьте "Volume" для сохранения `nano_banana.db`
3. Или используйте Postgres (лучше для продакшена)

---

## 💰 Стоимость

Railway работает по модели pay-as-you-go:

- **Первые $5** бесплатно каждый месяц
- Далее примерно **$5-10/месяц** для небольшого проекта
- Спит после неактивности (можно отключить)

---

## 🎉 Поздравляем!

Ваш Nano Banana проект работает в интернете 24/7!

Теперь можете:
- ✅ Делиться ссылкой на сайт
- ✅ Продвигать Telegram бота
- ✅ Продавать токены за Stars
- ✅ Монетизировать Gemini API

**Следующие шаги:**
1. Настройте красивое доменное имя (опционально)
2. Добавьте логотип и брендинг
3. Расширьте функционал
4. Привлекайте пользователей!

Удачи! 🚀🍌

