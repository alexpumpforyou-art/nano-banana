# Nano Banana (Telegram & Web): История проекта и инструкция «для себя»

## Основная идея
Сервис позволяет пользователям в Telegram-боте и на вебе покупать токены (за Stars) и генерировать текст/изображения (и редактировать фото) с помощью Google Gemini (Nano Banana). Продажа и учёт токенов, интеграция Gemini, веб-интерфейс и надёжная работа на Railway.

---

## 1. ⏪ История шаг за шагом

- **Старт**: Поставлена задача — сделать платформу генерации текста/картинок через Gemini с Telegram-ботом и продаваемыми токенами.
- **Требования**: Сервер (Express JS), API-ключи (Telegram, Gemini), база (SQLite), web-UI, токен-система, деплой на дешевом VPS/PAAS.
- **Деплой**: Выбор Railway. GitHub-репозиторий — транспорт обновлений.
- **Работа с Gemini**: Проблемы с API-версиями (v1/v1beta), квотами, моделями (Deprecated, 404, Quota, Overload). Фолбек моделей (автоматический перебор рабочей).
- **Продажи токенов**: Реализация покупок через Stars, учёт баланса, история транзакций.
- **Телеграм-бот**: Интеграция, команды, генерация текста, картинок, поддержка платёжки.
- **Генерация и РЕДАКТИРОВАНИЕ картинок**: Различие между генерацией (промпт на картинку) и редактированием (фото + запрос на изменение). Исправлено так, чтобы всегда редактировалось именно присланное изображение, а не генерировалось новое с нуля.
- **Документация и поддержка**: Подробные инструкции по развертыванию (README, QUICKSTART, DEPLOY_NOW, RAILWAY_STEP_BY_STEP и др).

---

## 2. 🗝️ Ключевые технические моменты

- **Express Backend**: REST endpoints, интеграция с Gemini, работа с Telegram через node-telegram-bot-api.
- **SQLite**: Хранение пользователей, истории платежей, запросов к Gemini (для статистики/отладки).
- **Gemini**: Работа через @google/generative-ai с fallback моделей для текста и изображений.
- **Telegram-бот**: Команды `/start`, `/balance`, `/buy`, `/help`, обычные сообщения (для генерации и редактирования), интеграция платежей.
- **Frontend (web)**: Простая форма генерации, REST API для связи с бэкендом.
- **Intent (детекция что хочет пользователь)**: Любое фото + текст = запрос на редактирование. Только текст — генерация. Промпт для Gemini обязательно уточняет «отредактируй это, не создавай новое изображение».
- **Логирование ошибок**: В случае 404/429/503 всегда смотри логи (отработка fallback моделей).

---

## 3. 🚦 Инструкция для сопровождения и отладки

### Быстрый старт:

1. Клонируй репозиторий → настрой .env (ключи Telegram/Gemini).
2. `git push` для деплоя на Railway.
3. В логах Railway проверь запуск, отлавливай ошибки в Gemini/Telegram тут же.

### Как тестировать:
- В боте: отправь фото с подписью — будет редактирование изображения. Отправь только текст — генерация текста/фото по содержанию промпта. Попробуй команды баланса/покупок/истории.
- В web: генерация текстов, просмотр и пополнение баланса.

### Что делать при ошибках:
- Следи за актуальностью @google/generative-ai — часто обновляются модели.
- Используй `/api/debug/models` для списка доступных моделей.
- При ошибках (404 — модель не найдена, 429 — квота, 503 — перегруз) всегда ищи новые модели для фолбека.
- Не хватает квоты — подключи другой ключ/модель.

### Для поддержки/допиливания:
- генерация/фолбек — image-service.js, gemini-service.js
- logic Telegram-бота — telegram-bot.js
- тесты и подбор новых моделей — list-models.js и тестовые скрипты
- визуал/web — папка public

---

## 4. 📌 Выжимка (TL;DR)

- Фото+текст = редактируй картинку (основа — ваш файл)
- Только текст = генерация (текст/картинка)
- Всё хранится в SQLite
- Fallback моделей Gemini для отказоустойчивости
- Деплой — git push → Railway
- Документация актуализируется при каждом значимом обновлении!






